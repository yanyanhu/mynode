sudo: required

language: node_js
node_js:  

branches:
    - master

services:
    - docker

before_script:
    - echo "Before test"
script:
    - echo "Hello, Travis" && ./test/travis/scripts/test.sh
after_script:
    - echo "After test"
